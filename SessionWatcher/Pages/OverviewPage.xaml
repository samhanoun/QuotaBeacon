<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SessionWatcher.Pages.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:presentation="using:SessionWatcher.Core.Presentation"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton
                AutomationProperties.Name="Refresh provider usage"
                Click="OnRefreshClicked"
                Icon="Refresh"
                Label="Refresh" />
        </CommandBar>

        <ScrollViewer Grid.Row="1">
            <Grid
                MaxWidth="1040"
                Padding="32,16,32,32"
                HorizontalAlignment="Stretch">
                <StackPanel Spacing="16">
                    <StackPanel Spacing="4">
                        <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="Usage overview" />
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Provider-reported quota windows stay separate from local fallback data."
                            TextWrapping="Wrap" />
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="{x:Bind ViewModel.StatusText, Mode=OneWay}" />
                    </StackPanel>

                    <ProgressBar
                        AutomationProperties.Name="Refreshing provider usage"
                        IsIndeterminate="True"
                        ShowError="False"
                        Visibility="{x:Bind ViewModel.IsRefreshing, Mode=OneWay}" />

                    <InfoBar
                        IsClosable="False"
                        IsOpen="{x:Bind ViewModel.HasNoProviders, Mode=OneWay}"
                        Message="No providers are registered. Check the Plugins page."
                        Severity="Informational"
                        Title="Nothing to show" />

                    <ItemsControl ItemsSource="{x:Bind ViewModel.Providers, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="presentation:ProviderCardModel">
                                <Border
                                    Margin="0,0,0,12"
                                    Style="{StaticResource ProviderCardStyle}">
                                    <StackPanel Spacing="14">
                                        <Grid ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Spacing="2">
                                                <TextBlock
                                                    Style="{StaticResource SectionTitleTextStyle}"
                                                    Text="{x:Bind ProviderName}" />
                                                <TextBlock
                                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                    Text="{x:Bind FreshnessText}" />
                                            </StackPanel>
                                            <TextBlock
                                                Grid.Column="1"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Text="{x:Bind PlanText}"
                                                VerticalAlignment="Center" />
                                        </Grid>

                                        <InfoBar
                                            IsClosable="False"
                                            IsOpen="{x:Bind HasDiagnostic}"
                                            Message="{x:Bind Diagnostic}"
                                            Severity="Warning" />

                                        <ItemsControl ItemsSource="{x:Bind Quotas}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="presentation:QuotaCardModel">
                                                    <Grid Padding="0,10,0,12" RowSpacing="8">
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                        </Grid.RowDefinitions>
                                                        <Grid ColumnSpacing="12">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock FontWeight="SemiBold" Text="{x:Bind Label}" />
                                                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                                                <TextBlock Text="{x:Bind UsedText}" />
                                                                <TextBlock
                                                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                    Text="{x:Bind RemainingText}" />
                                                            </StackPanel>
                                                        </Grid>
                                                        <ProgressBar
                                                            Grid.Row="1"
                                                            AutomationProperties.Name="{x:Bind AccessibleSummary}"
                                                            Maximum="100"
                                                            Value="{x:Bind UsedPercent}" />
                                                        <Grid Grid.Row="2" ColumnSpacing="12">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock
                                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                Text="{x:Bind ResetText}" />
                                                            <TextBlock
                                                                Grid.Column="1"
                                                                FontWeight="SemiBold"
                                                                Text="{x:Bind PaceText}" />
                                                        </Grid>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
