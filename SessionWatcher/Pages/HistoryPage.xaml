<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SessionWatcher.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:SessionWatcher.ViewModels"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton Click="OnReloadClicked" Icon="Refresh" Label="Reload history" />
        </CommandBar>
        <Grid Grid.Row="1" MaxWidth="1040" Padding="32,16,32,32" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Margin="0,0,0,16" Spacing="4">
                <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="Usage history" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Sanitized quota snapshots are kept locally for up to 90 days."
                    TextWrapping="Wrap" />
            </StackPanel>
            <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.History, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:HistoryRowModel">
                        <StackPanel Padding="8,12" Spacing="3">
                            <TextBlock FontWeight="SemiBold" Text="{x:Bind ProviderName}" />
                            <TextBlock Text="{x:Bind Summary}" TextWrapping="Wrap" />
                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                <Run Text="{x:Bind ObservedText}" />
                                <Run Text=" · " />
                                <Run Text="{x:Bind SourceText}" />
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
